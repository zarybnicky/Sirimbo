import '!style-loader!css-loader!resolve-url-loader!sass-loader!leaflet/dist/leaflet.css';

export const parameters = {
  actions: { argTypesRegex: "^on[A-Z].*" },
  controls: {
    matchers: {
      color: /(background|color)$/i,
      date: /Date$/,
    },
  },
}
