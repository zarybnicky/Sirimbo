apiVersion: v1
kind: Service
metadata:
  name: sirimbo
  labels:
    app.kubernetes.io/name: sirimbo
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: sirimbo
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
