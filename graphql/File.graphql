fragment File on File {
  id
  name
  displayName
  url
  contentType
  uploadedAt
  byteSize
  isPublic
}

query FileList {
  files(first: 500, orderBy: [UPLOADED_AT_DESC, ID_DESC]) {
    nodes {
      ...File
    }
  }
}
