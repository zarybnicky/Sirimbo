<div class="trenink-header">
  <div class="h5 mb-0">
    {{ item.u_jmeno }} {{ item.u_prijmeni }}
    {% if item.canEdit %}
      <div class="btn-group">
        <button type="button" class="btn btn-xs pt-0" data-toggle="dropdown">
          <img alt="Upravit" width="16" src="/style/icon-gear.png" />
        </button>
        <div class="dropdown-menu dropdown-menu-right">
          <a class="dropdown-item" href="/admin/rozpis/edit/{{ item.r_id }}">Upravit</a>
          <a class="dropdown-item" href="/admin/rozpis/detail/{{ item.r_id }}">Upravit lekce</a>
        </div>
      </div>
    {% endif %}
  </div>
  <div class="date">{{ item.r_datum|date('j. n. Y') }}</div>
  <div>{{ item.r_kde }}</div>
</div>
