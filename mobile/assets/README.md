# Mobile assets

This directory intentionally omits binary icon and splash assets. Provide the platform-specific artwork (PNG/WebP) before building native bundles and update `app.json` if the filenames change.
