schema:
  - schema.graphql
  - "directive @requires(depends: String[]) on FIELD"

documents: "libs/graphql/*.graphql"
watch: "libs/graphql/*.graphql"
config:
  scalars:
    Datetime: "string"
    Date: "string"
    Time: "string"
    BigInt: "string"
    JSON: "{ [key: string]: unknown }"
