schema:
  - schema.graphql
  - "directive @requires(depends: String[]) on FIELD"

documents: "frontend/graphql/*.graphql"
watch: "./**/*.graphql"
config:
  scalars:
    Datetime: "string"
    Date: "string"
    Time: "string"
    BigInt: "string"
    JSON: "{ [key: string]: unknown }"
